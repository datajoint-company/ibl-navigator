import datajoint as dj

from ibl_pipeline.subject import schema as subject
from ibl_pipeline.action import schema as action
from ibl_pipeline.acquisition import schema as acquisition
# from ibl_pipeline.analyses.ephys import schema as analyses_ephys
# from ibl_pipeline.plotting.behavior import schema as plotting_behavior

# subject = dj.Schema('ibl_{}'.format('subject'))
# reference = dj.Schema('ibl_{}'.format('reference'))
# action = dj.Schema('ibl_{}'.format('action'))
# acquisition = dj.Schema('ibl_{}'.format('acquisition'))
# plotting_behavior = dj.Schema('ibl_{}'.format('plotting_behavior'))
# analyses_behavior = dj.Schema('ibl_{}'.format('analyses_behavior'))
# plotting_ephys = dj.Schema('ibl_{}'.format('plotting_ephys'))
# test_plotting_ephys = dj.Schema('test_ibl_{}'.format('plotting_ephys'))
# ephys = dj.Schema('ibl_{}'.format('ephys'))
